<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0E14">

    <androidx.camera.view.PreviewView
        android:id="@+id/previewView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/connectBar"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <org.webrtc.SurfaceViewRenderer
        android:id="@+id/webrtcRenderer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="@id/previewView"
        app:layout_constraintBottom_toBottomOf="@id/previewView"
        app:layout_constraintStart_toStartOf="@id/previewView"
        app:layout_constraintEnd_toEndOf="@id/previewView" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/switchCameraButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_margin="16dp"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        app:icon="@android:drawable/ic_menu_rotate"
        app:iconGravity="textStart"
        app:iconPadding="0dp"
        app:cornerRadius="28dp"
        android:backgroundTint="#1E2733"
        app:iconTint="#E8EDF4"
        android:contentDescription="Switch between front and rear camera"
        app:layout_constraintTop_toTopOf="@id/previewView"
        app:layout_constraintEnd_toEndOf="@id/previewView" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/orientationToggleButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:insetTop="0dp"
        android:insetBottom="0dp"
        app:icon="@android:drawable/ic_menu_crop"
        app:iconGravity="textStart"
        app:iconPadding="0dp"
        app:cornerRadius="28dp"
        android:backgroundTint="#1E2733"
        app:iconTint="#E8EDF4"
        android:contentDescription="Toggle landscape/portrait streaming orientation"
        app:layout_constraintTop_toBottomOf="@id/switchCameraButton"
        app:layout_constraintEnd_toEndOf="@id/previewView" />

    <TextView
        android:id="@+id/statusText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:paddingBottom="4dp"
        android:textColor="#8A97A8"
        android:text="Not connected — waiting for C4D Portal desktop app"
        app:layout_constraintBottom_toTopOf="@id/connectBar" />

    <LinearLayout
        android:id="@+id/connectBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="12dp"
        android:gravity="center_vertical"
        app:layout_constraintBottom_toBottomOf="parent">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/desktopAddressInput"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Desktop IP:port (e.g. 192.168.1.50:8787)"
            android:textColor="#E8EDF4"
            android:textColorHint="#5A6576"
            android:inputType="text"
            android:background="@android:color/transparent"
            android:padding="10dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/connectUsbButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="8dp"
            android:text="USB"
            android:backgroundTint="#1E2733" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/connectButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Connect"
            android:backgroundTint="#EF3B3B" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
